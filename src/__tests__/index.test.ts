// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { srand, rand } from '..'

describe('srand', () => {
  it('is true', () => {
    expect.assertions(1)
    expect(true).toBeTruthy()
  })
})
